# Copy to .env and fill in, or export in your shell.
# Get a key at https://console.anthropic.com/  (or run `ant auth login` and skip this)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: every exercise reads this. Default is claude-opus-5.
# CLAUDE_MODEL=claude-opus-5
